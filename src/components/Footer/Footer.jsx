import styles from './styles.module.css';

function Footer(){
    return (
    <footer className={styles.footer}>
        <p>Crafted with passion by <b>Aadil Khan</b> — Turning ideas into visuals.</p>
    </footer>);
}

export default Footer;